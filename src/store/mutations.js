export default {
  POSTS(state, posts) {
    state.posts = posts
  },
  POST(state, post) {
    state.post = post
  },
  CONFIG(state, config) {
    state.config = config
  },
  CURRENT(state, current) {
    state.current = current
  }
}